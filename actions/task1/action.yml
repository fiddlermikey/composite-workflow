name: 'Hello Folks'
description: 'Say HI!'
inputs:
  who-to-say-Hi:  # id of the input
    description: 'Who to say Hi'
    required: true
    default: ‘Folks’
outputs:
  greeting:
    description: "Greeting"
    value: ${{ inputs.who-to-say-Hi }}
runs:
  using: "composite"
  steps:
    - run: echo "Hello ${{ inputs.who-to-say-Hi }}."
      shell: bash